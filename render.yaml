services:
  - type: web
    name: clausio-api
    env: python
    region: singapore        # closest to Chennai
    plan: free               # works now — no GiNZA loading at runtime
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
